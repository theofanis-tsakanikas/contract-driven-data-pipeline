# Marts BI Dashboard — Streamlit dependencies.
# Light and self-contained: demo mode needs no Airflow/Spark/S3/Postgres.

streamlit>=1.40
pandas>=2.0
plotly>=5.20

# Optional — only needed for "Postgres (live)" mode:
psycopg2-binary>=2.9
